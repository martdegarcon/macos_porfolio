Изучаю структуру проекта, чтобы подготовить README:


[4 tools called]


README для проекта:

```markdown
# macOS Portfolio

Интерактивное портфолио, стилизованное под macOS, с перетаскиваемыми окнами, анимациями и современным UI.

## 🎨 Особенности

- **macOS-стилизованный интерфейс** - точная копия интерфейса macOS с Dock, окнами и навигацией
- **Перетаскиваемые окна** - все окна можно перетаскивать по экрану (GSAP Draggable)
- **Анимации** - плавные анимации открытия/закрытия окон с помощью GSAP
- **Интерактивный Finder** - файловая система с проектами, файлами и папками
- **Различные типы окон**:
  - **Finder** - файловый менеджер с проектами
  - **Safari** - блог с постами
  - **Terminal** - список технологий
  - **Resume** - PDF резюме с возможностью скачивания
  - **Contact** - контакты и социальные сети
  - **Text** - текстовые файлы с описаниями проектов
  - **Image** - просмотр изображений
- **Dock** - панель внизу экрана с приложениями
- **Home Screen** - главный экран с папками проектов

## 🚀 Технологии

- **React 19** - UI библиотека
- **Vite** - сборщик и dev-сервер
- **Zustand** - state management с Immer middleware
- **GSAP** - анимации и перетаскивание окон
- **Tailwind CSS 4** - стилизация
- **Lucide React** - иконки
- **React PDF** - просмотр PDF файлов
- **Day.js** - работа с датами
- **React Tooltip** - подсказки

## 📦 Установка

1. Клонируй репозиторий:
```bash
git clone <repository-url>
cd macos-portfolio
```

2. Установи зависимости:
```bash
npm install
```

3. Запусти dev-сервер:
```bash
npm run dev
```

4. Открой браузер по адресу `http://localhost:5173`

## 🛠️ Доступные команды

- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка для production
- `npm run preview` - предпросмотр production сборки
- `npm run lint` - проверка кода ESLint

## 📁 Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
│   ├── Dock.jsx        # Dock панель внизу экрана
│   ├── Home.jsx        # Главный экран с папками
│   ├── Navbar.jsx      # Верхняя панель навигации
│   ├── Welcome.jsx      # Приветственный экран
│   └── WindowControls.jsx  # Кнопки управления окнами
├── windows/            # Компоненты окон
│   ├── Finder.jsx      # Файловый менеджер
│   ├── Safari.jsx      # Браузер/Блог
│   ├── Terminal.jsx    # Терминал с технологиями
│   ├── Resume.jsx      # PDF резюме
│   ├── Contact.jsx     # Контакты
│   ├── Text.jsx        # Текстовые файлы
│   └── Image.jsx       # Изображения
├── store/              # Zustand stores
│   ├── window.js       # Управление окнами
│   └── location.js     # Управление локациями в Finder
├── hoc/                # Higher-Order Components
│   └── windowWrapper.jsx  # Обертка для окон с анимациями
├── constants/          # Константы и данные
│   └── index.js        # Данные проектов, локаций, навигации
└── App.jsx             # Главный компонент
```

## 🎯 Основные концепции

### State Management (Zustand)

Проект использует два основных store:

1. **Window Store** (`src/store/window.js`)
   - Управление открытием/закрытием окон
   - Управление z-index для фокуса окон
   - Передача данных в окна

2. **Location Store** (`src/store/location.js`)
   - Управление активной локацией в Finder
   - Переключение между папками и проектами

### Window System

Все окна обернуты в `windowWrapper` HOC, который:
- Добавляет анимации открытия/закрытия
- Делает окна перетаскиваемыми
- Управляет видимостью на основе `isOpen` из store

### Finder Navigation

Finder использует систему локаций:
- **Favorites** - основные папки (Work, About, Resume, Trash)
- **My Projects** - проекты из Work локации
- Клик на папку меняет `activeLocation` и показывает её содержимое

## 🎨 Кастомизация

### Добавление нового проекта

Отредактируй `src/constants/index.js`:

```javascript
const WORK_LOCATION = {
  // ...
  children: [
    {
      id: 7,
      name: "My New Project",
      icon: "/images/folder.png",
      kind: "folder",
      position: "top-10 left-5",
      children: [
        // файлы проекта
      ]
    }
  ]
}
```

### Добавление нового окна

1. Создай компонент в `src/windows/YourWindow.jsx`
2. Оберни его в `windowWrapper`:
```jsx
const YourWindow = windowWrapper(YourComponent, 'yourwindow')
export default YourWindow
```
3. Добавь в `WINDOW_CONFIG` в `src/constants/index.js`:
```javascript
yourwindow: { isOpen: false, zIndex: INITIAL_Z_INDEX, data: null }
```
4. Импортируй и добавь в `App.jsx`

### Изменение стилей

Стили находятся в `src/index.css` с использованием Tailwind CSS и кастомных utility классов:
- `flex-center` - центрирование по горизонтали и вертикали
- `col-center` - вертикальное центрирование
- `abs-center` - абсолютное центрирование

## 📝 Лицензия

MIT

## 👤 Автор

Martgarcon Portfolio

---

Сделано с ❤️ используя React и GSAP
```

Этот README описывает проект, технологии, структуру и способы кастомизации. Если нужно что-то добавить или изменить, скажи.